<li>
  {{#link-to 'show' show.id}}{{show.title}}{{/link-to}}
  {{#if isImageShowing}}
    <p><img src={{show.image}} {{action 'imageHide'}}></p>
  {{else}}
    <p {{action 'imageShow'}}>Show Poster</p>
  {{/if}}
</li>
