{{#power-calendar
  class="demo-calendar-big"
  selected=selected
  center=month
  onSelect=(action (mut selected) value="moment")
  onCenterChange=(action (mut month) value="date") as |calendar|}}
  {{calendar.nav class="nav"}}
  {{calendar.days}}
{{/power-calendar}}

{{yield}}
